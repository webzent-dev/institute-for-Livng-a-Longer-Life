name: Deploy to Production

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
    types: [closed]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.pull_request.merged == true)
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install npm dependencies
      run: npm ci
      
    - name: Build assets
      run: npm run build
      
    - name: Clean up node_modules
      run: rm -rf node_modules
      
    - name: Get changed files
      id: changed-files
      uses: tj-actions/changed-files@v40
      with:
        files: |
          **/*.php
          **/*.js
          **/*.css
          **/*.html
          **/*.json
          public/**
          resources/**
          app/**
          config/**
          database/**
          routes/**
          
    - name: List all changed files
      run: |
        echo "=== DEPLOYMENT DEBUG INFO ==="
        echo "Any changed files: ${{ steps.changed-files.outputs.any_changed }}"
        echo "All changed files: ${{ steps.changed-files.outputs.all_changed_files }}"
        echo "Added files: ${{ steps.changed-files.outputs.added_files }}"
        echo "Modified files: ${{ steps.changed-files.outputs.modified_files }}"
        echo "Deleted files: ${{ steps.changed-files.outputs.deleted_files }}"
        echo "=========================="
        
    - name: Create deployment directory with changed files
      if: steps.changed-files.outputs.any_changed == 'true'
      run: |
        echo "=== CREATING DEPLOYMENT DIRECTORY ==="
        mkdir -p deploy
        echo "Created deploy directory"
        
        # List files to be copied
        echo "Files to copy:"
        echo "${{ steps.changed-files.outputs.all_changed_files }}" | tr ' ' '\n' | while read file; do
          if [ -n "$file" ] && [ -f "$file" ]; then
            echo "  - $file"
            mkdir -p "deploy/$(dirname "$file")"
            cp "$file" "deploy/$file"
          fi
        done
        
        # Show deploy directory contents
        echo "Deploy directory contents:"
        find deploy -type f | head -20
        echo "======================================"
        
    - name: Deploy only changed files to server via FTP
      if: steps.changed-files.outputs.any_changed == 'true' || true
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: ftp.bikewrapt.com
        username: living@bikewrapt.com
        password: webzent@123
        server-dir: /
        protocol: ftp
        port: 21
        local-dir: deploy/
        dangerous-clean-slate: false
        sync: false
        state-name: .ftp-deploy-sync-state.json
        exclude: .git/**, .git*, .github/**, node_modules/**, .env*, storage/logs/**, storage/framework/cache/**, storage/framework/sessions/**, storage/framework/testing/**, storage/framework/views/**, bootstrap/cache/**, vendor/**, old/**, tests/**, .gitignore, README.md, artisan, composer.json, composer.lock, package.json, package-lock.json, phpunit.xml, postcss.config.js, tailwind.config.js, npm, .editorconfig, .gitattributes
